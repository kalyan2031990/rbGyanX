pydicom>=2.4.0,<3.0  # dicompyler-core 0.5.6 needs pydicom<3.0 (pixel_data_handlers removed in 3.0)
dicompyler-core>=0.5.5
# NOTE: do NOT install the legacy `dicom` (pydicom 0.9.x) package — it shadows
# pydicom in dicompyler-core's import shim and breaks all real-DICOM DVH extraction.
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0
scipy>=1.10.0
pytest>=7.0.0
pyyaml>=6.0
scikit-learn>=1.3
lifelines>=0.27
xgboost>=1.7
lightgbm>=3.3
shap>=0.41
joblib>=1.2
matplotlib>=3.7
lime>=0.2
tabulate>=0.9
